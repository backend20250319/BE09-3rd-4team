spring:
  application:
    name: movie-service
  cloud:
    config:
      uri: http://localhost:8888 # Config Server 주소
  datasource:
    url: jdbc:mysql://mysql.coffit.xyz/moviedb
    username: smile
    password: smile
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
